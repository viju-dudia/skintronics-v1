# Copy to .env and fill in locally. Never commit or share the Key Secret.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# Set the same secret when configuring the Razorpay webhook.
RAZORPAY_WEBHOOK_SECRET=

# Explicitly enable after confirming product availability and these amounts.
CHECKOUT_ENABLED=false
# This integration supports INR. Amounts below are integer paise, not rupees.
# Leave unknown values blank. Use 0 only when no extra charge is confirmed.
CHECKOUT_CURRENCY=INR
PRODUCT_PRICE_PAISE=
SHIPPING_PAISE=
TAX_PAISE=
# One confirmed delivery country (two-letter ISO code, for example IN).
SHIPPING_COUNTRY=

APP_ORIGIN=http://127.0.0.1:4173
HOST=127.0.0.1
PORT=4173
# Private durable directory. Keep this outside any other web server's root.
ORDER_DATA_DIR=.data/orders
